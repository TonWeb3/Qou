# ── Quotex API (installed from GitHub — the PyPI name is different) ──
git+https://github.com/cleitonleonel/pyquotex.git

# ── Quotex API dependencies ───────────────────────────────────────────
websockets
aiohttp
cloudscraper
requests

# ── Telegram ──────────────────────────────────────────────────────────
telethon
cryptg

# ── Web dashboard ─────────────────────────────────────────────────────
flask
flask-socketio
simple-websocket

# ── Utilities ─────────────────────────────────────────────────────────
certifi
pytz

# Browser TLS fingerprint for the Quotex WebSocket. Without it Cloudflare
# rejects the handshake with HTTP 403 and the bot cannot trade at all.
curl_cffi>=0.13.0
